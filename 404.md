---
layout: page
---

<script setup>
import { useRouter } from 'vitepress'

const router = useRouter()
</script>

<style scoped>
.not-found {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 48px 24px 64px;
  text-align: center;
  overflow: hidden;
}

/* 迷路的小猫 */
.cat-emoji {
  font-size: 80px;
  animation: catWalk 3s ease-in-out infinite;
  margin-bottom: 8px;
}

@keyframes catWalk {
  0%, 100% { transform: translateX(0) rotate(0deg); }
  25% { transform: translateX(-20px) rotate(-5deg); }
  75% { transform: translateX(20px) rotate(5deg); }
}

/* 浮动的小装饰 */
.floating-decor {
  display: flex;
  gap: 24px;
  margin-bottom: 16px;
  font-size: 28px;
}

.floating-decor span:nth-child(1) {
  animation: float 2.5s ease-in-out infinite;
}
.floating-decor span:nth-child(2) {
  animation: float 2.5s ease-in-out infinite 0.5s;
}
.floating-decor span:nth-child(3) {
  animation: float 2.5s ease-in-out infinite 1s;
}

@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-12px); }
}

/* 404 数字 */
.error-code {
  font-size: 120px;
  font-weight: 800;
  line-height: 1;
  background: linear-gradient(135deg, #ff6b9d, #c084fc, #60a5fa);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 12px;
  animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.03); }
}

/* 文案 */
.message {
  font-size: 22px;
  font-weight: 600;
  color: var(--vp-c-text-1);
  margin-bottom: 8px;
}

.sub-message {
  font-size: 15px;
  color: var(--vp-c-text-3);
  margin-bottom: 36px;
  line-height: 1.6;
}

/* 按钮组 */
.actions {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  justify-content: center;
}

.action-btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 12px 28px;
  font-size: 15px;
  font-weight: 500;
  border-radius: 24px;
  cursor: pointer;
  transition: all 0.3s ease;
  text-decoration: none;
  border: none;
}

.action-btn.primary {
  color: var(--vp-button-brand-text);
  background: linear-gradient(135deg, #ff6b9d, #c084fc);
  box-shadow: 0 4px 16px rgba(192, 132, 252, 0.3);
}

.action-btn.primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 24px rgba(192, 132, 252, 0.45);
}

.action-btn.secondary {
  color: var(--vp-c-text-1);
  background: var(--vp-c-bg-soft);
  border: 1px solid var(--vp-c-divider);
}

.action-btn.secondary:hover {
  transform: translateY(-2px);
  border-color: var(--vp-c-brand-1);
  color: var(--vp-c-brand-1);
}

/* 底部小彩蛋 */
.easter-egg {
  margin-top: 48px;
  font-size: 13px;
  color: var(--vp-c-text-3);
  opacity: 0.6;
}
</style>

<div class="not-found">
  <div class="cat-emoji">🐱</div>
  <div class="floating-decor">
    <span>✨</span>
    <span>🔍</span>
    <span>✨</span>
  </div>
  <div class="error-code">404</div>
  <div class="message">哎呀，小猫找不到这个页面了~</div>
  <div class="sub-message">
    页面可能搬家了、被删除了，或者链接写错了 🤔<br/>
    别担心，让我们帮你找到回去的路！
  </div>
  <div class="actions">
    <a class="action-btn primary" href="/" @click.prevent="router.go('/')">
      🏠 回到首页
    </a>
    <a class="action-btn secondary" href="/guide/" @click.prevent="router.go('/guide/')">
      📖 查看教程
    </a>
  </div>
  <div class="easter-egg">💡 提示：试试页面顶部的搜索功能，也许能找到你想要的~</div>
</div>
